export const signup = (req, res) => {
  res.json({
    data: "singup endpoint"
  })
};

export const login = (req, res) => {
  res.json({
    data: "login endpoint"
  })
};

export const logout = (req, res) => {
  res.json({
    data: "logout endpoint"
  })
};